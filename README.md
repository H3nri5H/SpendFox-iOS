# SpendFox iOS

Native iOS application for private expense tracking, products and vehicles.

The project is built with SwiftUI and stores all prototype data locally with SwiftData. Cloud synchronization is intentionally deferred until the local domain model and user flows are stable.
